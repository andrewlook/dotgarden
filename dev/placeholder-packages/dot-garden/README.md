# dot-garden — placeholder

This package is a **placeholder** published to prevent typosquatting of the
real [`dotgarden`](https://pypi.org/project/dotgarden/) package.

## Real package

```bash
pip install dotgarden
```

Importing this placeholder raises `RuntimeError`.
